{
  "id": "appearance.vas_vertical_appearance",
  "title": "VAS (Visual Analog Scale) fields render with a vertical appearance",
  "kind": "hybrid",
  "scope": "global",
  "status": "active",
  "natural_key": "vas_vertical_appearance",
  "description": "OC's internal Participate Form Design best-practice doc: 'For VAS Scales, use a vertical appearance instead of horizontal.' DEFAULT: any Visual Analog Scale field (typically a 0-100 numeric slider for pain, fatigue, quality-of-life) uses a vertical appearance. The exact appearance keyword is OC-version-specific — note as appearance='vas vertical' or appearance='distress vertical' per the OC widget convention in use, and flag for verification at first encounter. TRIGGER DETECTION: field has appearance=distress, appearance=vas, or any other VAS-style widget keyword. Override: customer library specifies horizontal explicitly. This is a narrow rule that fires only when a VAS exists; most CDASH-domain protocols don't have one.",
  "rationale": "Horizontal VAS sliders on mobile portrait screens render with significantly less precision than vertical — the slider track has only a few hundred pixels of width to map to a 0-100 scale, making fine-grained subject responses (e.g. 'my pain is 73') impractical. Vertical VAS uses the full screen height, dramatically improving precision and reducing accidental edge selections. Patient-reported outcomes that use VAS (pain, distress, fatigue) particularly benefit from vertical rendering.",
  "target": "field",
  "applies_when": {
    "any_of": [
      { "field.appearance": "vas" },
      { "field.appearance": "distress" },
      { "field.appearance": { "matches": "vas" } },
      { "field.appearance": { "matches": "distress" } }
    ]
  },
  "effect": {
    "flag": {
      "category": "review_flags.vas_orientation",
      "message": "Field ${field.name} has VAS appearance (current: ${field.appearance}) — render with vertical orientation per OC's Participate Form Design best-practice. Update to 'vas vertical' or 'distress vertical' per the OC widget convention. Verify the exact appearance keyword against the OC version in use at first encounter."
    },
    "soft": "When generating VAS fields (or detecting them via appearance=vas/distress), append 'vertical' to the appearance. The exact form of the appearance keyword differs slightly across OC versions — flag for verification on first build. Surface in conventions_applied.vas_appearance_rule.{vas_fields, vas_vertical}."
  },
  "created_at": "2026-05-15T00:00:00Z",
  "created_by": "system:phase_b1d_translation",
  "source": "consolidation:skills/protocol-analysis/references/conventions.md:§18",
  "tags": ["appearance", "vas", "mobile_optimization", "patient_reported_outcomes"],
  "history": [
    {
      "at": "2026-05-15T00:00:00Z",
      "by": "system:phase_b1d_translation",
      "action": "created",
      "status": "active",
      "note": "Imported from conventions.md §18 during Phase B.1d. Hybrid because the appearance-keyword variant (vas vertical vs distress vertical) differs across OC versions and requires version-aware string manipulation that isn't a clean DSL effect. The set directive could handle the simple case (literal string append) but the version-conditional variant fits soft better. The trigger detection IS structured (matches on appearance keyword)."
    }
  ]
}
