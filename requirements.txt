fastapi==0.115.0
uvicorn==0.30.0
httpx==0.27.0
python-multipart==0.0.9
anthropic>=0.96.0
python-dotenv==1.0.0
openpyxl==3.1.5
reportlab==4.4.10
pyxform>=2.1.0
# Word-doc protocol fallback (.docx text extraction when LibreOffice
# isn't available in the container). See _extract_docx_as_text in pipeline.py.
python-docx>=1.1.0

# --- Build Preview dependencies ---
# playwright version must match the Chromium revision in the
# Microsoft Playwright Docker image (v1.49.0-jammy). If you bump the image
# version in the Dockerfile, bump this pin to the matching version.
playwright==1.49.0
pypdf>=4.0

# --- UAT runner integration (DVS XLSX → ODM XML → OC import) ---
# Pinned to a specific commit so Docker's `pip install` layer-cache
# invalidates whenever the upstream code moves. To upgrade: update the
# @<sha> below; pip will re-resolve and rebuild this layer.
oc-uat-runner @ git+https://github.com/dswanker/oc-uat-runner.git@d898079

# --- OAuth authentication for per-user form uploads ---
authlib>=1.3.0
itsdangerous>=2.1.0
